import { LargeDirective } from './large.directive';

describe('LargeDirective', () => {
  it('should create an instance', () => {
    const directive = new LargeDirective();
    expect(directive).toBeTruthy();
  });
});
