import { RichTooltipDirective } from './rich-tooltip.directive';

describe('RichTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new RichTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
